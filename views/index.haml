%div{:class => [:hero, :dark, :row]}
  %p
    %big{:class => :miles} 0
    of
    %span{:class => :goal} 0
    miles this week.
  %p
    %big{:class => :trend} 0
    %span{:class => :trend_desc} more miles than last week.
  %p{:style => "flex: 1"}
    %big On track
    to hit goal by
    %span{:class => :goal_date} January 1.
  %div{:class => [:graph, :row]}
%div{:class => ["master-detail", :row]}
  .master
    .empty
      %p You haven&rsquo;t gone for a run yet.
      %button Get the app
    %ul.list
  .detail
    .empty
      %p Once you go for a run, refresh to see details here.
      %button See a sample
    #output
%div{:class => [:footer, :dark]}
  %span
    GET your data at
    %a{:href => "#{base}api/#{db.id}?user=#{api_key.api_key}&amp;token=#{api_key.token}"} #{base}api/#{db.id}?user=#{api_key.api_key}&amp;token=#{api_key.token}
<!-- Cloudant URL -->
<!-- #{db.api.chomp('/')}/#{db.table} -->
